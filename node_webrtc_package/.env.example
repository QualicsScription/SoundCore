# Copy this to .env and adjust as needed
# Server bind
PORT=3000
HOST=0.0.0.0

# Comma-separated list of allowed origins for CORS (server and Socket.IO)
# Keep your ngrok domain here by default
CORS_ORIGINS=https://hyena-close-purely.ngrok-free.app,http://localhost:3000,http://127.0.0.1:3000

# Optional: If you host Socket.IO on a different origin, set it here and reference in public/config.js as window.__SIGNAL_URL
# SIGNAL_URL=https://hyena-close-purely.ngrok-free.app

# Optional TURN credentials (recommended for strict NATs) - specify in public/config.js as window.__ICE
# TURN_URLS=turn:turn.example.com:3478
# TURN_USERNAME=your_user
# TURN_CREDENTIAL=your_password