# Frontend Environment Example
# IMPORTANT: Do not hardcode backend URLs in code. Use this variable only.
# The value should point to the backend with /api prefix routing.
# Example with ingress that maps /api to FastAPI backend:
# REACT_APP_BACKEND_URL=https://your-domain.com/api

REACT_APP_BACKEND_URL=https://your-domain.com/api
WDS_SOCKET_PORT=443